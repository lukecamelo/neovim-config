require('ibl').setup({
	-- this highlights the leftmost indentation line for the current context (e.g. function body)
	-- show_current_context = true,	
	-- this underlines the current context (e.g. the function head)
    -- show_current_context_start = true
})

